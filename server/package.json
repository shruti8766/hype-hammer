{
  "name": "hypehammer-server",
  "version": "1.0.0",
  "type": "module",
  "description": "Backend server for HypeHammer data persistence",
  "main": "app.py",
  "scripts": {
    "start": "python app.py",
    "dev": "python -m flask --app app run --debug"
  },
  "dependencies": {
    "socket.io-client": "^4.8.3"
  },
  "python": {
    "requirements": [
      "flask==3.0.0",
      "flask-cors==4.0.0",
      "firebase-admin==6.2.0",
      "python-dotenv==1.0.0"
    ]
  }
}
